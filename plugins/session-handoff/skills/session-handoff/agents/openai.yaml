interface:
  display_name: "Session Handoff"
  short_description: "Prepare and verify safe cross-session work handoffs"
  default_prompt: "Use $session-handoff to prepare or recover this work safely."
